# Application
APP_NAME=Alpilab AI
APP_ENV=development
DEBUG=true

# AI — use "mock" until a real provider is configured
AI_PROVIDER=mock

# Database (not fully wired yet)
DATABASE_URL=sqlite:///./runtime_data/alpilab_ai.db

# Security — replace in local .env; never commit real secrets
SECRET_KEY=change-me-in-env

# Future provider keys (leave empty; do not commit real values)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_API_KEY=
