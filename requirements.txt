# Alpilab AI - foundation dependencies
# Keep this list minimal; add providers only when integrating for real.

pydantic>=2.0,<3
python-dotenv>=1.0,<2
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pytest>=8.0,<9
