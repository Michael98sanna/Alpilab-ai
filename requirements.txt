# Alpilab AI — minimal, mature dependencies only.
# No real AI provider SDKs in this foundation phase.

pydantic>=2.0,<3
python-dotenv>=1.0,<2
fastapi>=0.110,<1
uvicorn>=0.27,<1
httpx>=0.27,<1

# Dev / test
pytest>=8.0,<9
